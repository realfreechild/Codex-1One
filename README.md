# Reading List App

A simple browser-based reading list that supports:

- Adding books/articles with title + author
- Bookmark toggles for favorites
- Highlight toggles for priority items
- Persistent storage via `localStorage`

## Run locally

Open `index.html` in your browser.
